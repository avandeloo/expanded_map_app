<div class="well">
  <h1 id="title">Markers</h1>
</div>
<div class="well">
  <% @markers.each do |marker| %>
    <h2><a href="/markers/<%= marker.id %>" class = link><%= marker.nickname %></a></h2>
    <hr>
  <% end %>
  
</div>
